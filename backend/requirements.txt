# Aligned with backend architecture doc; start_prod_win.ps1 runs pip install -r before start.
# Manual: python -m pip install -r backend/requirements.txt (from repo root or backend)

fastapi>=0.100,<1
uvicorn[standard]>=0.24,<1
sqlalchemy>=2.0,<3
pymysql>=1.1,<3
pydantic-settings>=2,<3
python-dotenv>=1,<2
